# Steps to creating the robot code

This directory has various subdirectories that provide initial code samples for
the steps outlined in the main README file.

To keep the documentation as short as possible in the main README file, you can do
a directory comparison with the root directory and the _.../steps/step-**xx**_ subdirectry
to see what has changes